# AI Toolkit 云端部署指南

## 🚀 快速部署步骤

### 1. 基础更新（必须）
```bash
cd /path/to/ai-toolkit-easy2use
git pull origin main
```

### 2. 构建和启动（二选一）

#### 方案A：简单启动（适合测试）
```bash
cd ui
npm run build
npm start
```

#### 方案B：使用PM2（推荐，生产环境）
```bash
cd ui
npm run build
pm2 start npm --name "ai-toolkit-ui" -- start
pm2 save
```

### 3. 验证部署
访问 `http://your-server-ip:3000` 检查是否正常运行

## 📋 详细说明

### 依赖检查
- Node.js 18+ 必须安装
- npm 或 pnpm 包管理器
- Git 用于代码更新

### 构建 vs 开发模式
- `npm run dev` - 开发模式（本地开发用）
- `npm run build` + `npm start` - 生产模式（云端部署用）

### 使用PM2的好处
- 进程守护，崩溃自动重启
- 支持负载均衡
- 日志管理
- 开机自启动

### 防火墙设置
确保开放3000端口：
```bash
# Ubuntu/Debian
sudo ufw allow 3000

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --reload
```

## 🔄 后续更新
每次只需要执行：
```bash
git pull origin main
cd ui && npm run build && pm2 restart ai-toolkit-ui
```

## ⚠️ 注意事项
1. 确保服务器有足够的内存（建议8GB+）
2. 首次构建可能需要几分钟时间
3. 如果遇到权限问题，确保当前用户有项目目录的读写权限
4. 建议使用Nginx反向代理，配置HTTPS